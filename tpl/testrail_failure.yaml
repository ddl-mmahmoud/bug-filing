summary: {{ summary }}
project: DOM
issuetype: Bug
severity: {{ severity }}
affects_versions: [{{ version }}]
customer_impact: TBD
steps_to_replicate: |-
  Run e2e test: <{{ tc_link }}>

expected_outcome: |-
  The feature should be validated by the e2e test.

actual_outcome: |-
  {{ actual_outcome | default("") | indent(2) }}

description: |-
  <{{ tt_link }}>

  To run this test in your PR, run this command: `/test-e2e {{ catalog_test_type }} --tags=testrail({{ tc_id }})`

  {{ custom_config_text | indent(2) }}

components: [{{ component }}]
found_by: Automated Testcase
reporter: {{ reporter }}
eng_team: {{ eng_team }}
labels: [{{ ("blocking" if blocking else "not-blocking") if blocking is defined else blocking }}]
failure_reason: {{ failure_reason }}
