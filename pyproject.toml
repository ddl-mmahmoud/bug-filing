[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "bug-filing"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "requests",
    "testrail-api",
]

[project.scripts]
create-ticket = "bug_filing.create_ticket:main"
